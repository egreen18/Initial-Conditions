%% Section 1 - IC = 0 is the best case for sub-equilibrium conditions
IC = 0:2:10;
all_dOpt = zeros(1,length(IC));
for i = 1:length(IC)
    results = dOpt_birth_death(IC(i));
    bubblePlot(results);
    all_dOpt(i) = results.dOpt;
end

%% Section 1.1 - 
figure()
scatter(IC, all_dOpt, ')